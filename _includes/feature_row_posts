<div class="feature__wrapper">
    <div class="feature__item{% if include.type %}--{{ include.type }}{% endif %}">
      <div class="archive__item">
        {% if post.header.image %}{% assign image_path = post.header.image %}{% endif %}
        {% if post.header.overlay_image %}{% assign image_path = post.header.overlay_image %}{% endif %}
        {% if image_path != blank  %}
          <div class="archive__item-teaser">
	  <a href="{{ post.url | relative_url }}"><img src="{{ image_path | relative_url }}"></a>
          </div>
        {% endif %}

        <div class="archive__item-body">
            <b>{{ post.title }}</b>
	    <br>
	    <i class="far fa-calendar-alt"></i> &nbsp;&nbsp;<span>{{ post.date | date:"%b %d, %Y" }}</span> &nbsp;&nbsp;&nbsp;<a href="{{ post.url | relative_url }}" class="btn btn--primary btn--small tag__highlight">Learn More</a>
        </div>
      </div>
    </div>
</div>
