<div class="feature__wrapper">

  {% for f in site.posts limit:3 %}
    <div class="feature__item{% if include.type %}--{{ include.type }}{% endif %}">
      <div class="archive__item">
        {% if f.header.image %}
		{% assign image_path = f.header.image %}
	{% endif %}
        {% if f.header.overlay_image %}
		{% assign image_path = f.header.overlay_image %}
	{% endif %}
        {% if image_path != blank  %}
          <div class="archive__item-teaser">
	  <a href="{{ f.url | relative_url }}">
            <img src="{{ image_path | relative_url }}"
                 alt="{% if f.alt %}{{ f.alt }}{% endif %}">
	  </a>
          </div>
        {% endif %}

        <div class="archive__item-body">
          {% if f.title %}
            <b>{{ f.title }}</b><a href="{{ f.url | relative_url }}" class="btn btn--primary btn--small tag__highlight">Learn More</a>
          {% endif %}

          {% if f.excerpt %}
            <div class="archive__item-excerpt">
              {{ f.excerpt | markdownify | rstrip }}
            </div>
          {% endif %}
        </div>
      </div>
    </div>

  {% endfor %}

</div>
