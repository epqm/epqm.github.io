<div class="home__post__summary">
  <div class="archive__item">
    <div class="archive__item-teaser">
    {% if post.header.image %}
        <a href="{{ post.url }}"> <img src="{{ post.header.image | relative_url }}"> </a>
    {% else %}
        <a href="{{ post.url }}"> <img src="/assets/images/home2.svg"> </a>
    {% endif %}
    </div>

    <div class="archive__item-body">
      {% if post.title %}

      <p class="archive__item-title">
            <a href="{{ post.url }}"> <span class="recent__posts__title">{{ post.title }}</span></a></p>
      {% endif %}

      <div class="archive__item-excerpt">
    	<i class="far fa-calendar-alt fontawesome__icon"></i> <span>{{ post.date | date:"%B %d, %Y"  }} </span>
      </div>

      {% if post.excerpt %}
        <div class="archive__item-excerpt">
          <p><i class="fas fa-comment fontawesome__icon"></i> {{ post.excerpt }} </p>
        </div>
      {% endif %}

    </div>
  </div>
</div>
