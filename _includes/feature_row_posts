    <div class="home__post__summary">
      <div class="archive__item">
        {% if post.header.image %}
          <div class="archive__item-teaser">
            <a href="{{ post.url }}"> <img src="{{ post.header.image | relative_url }}"> </a>
          </div>
        {% endif %}

        <div class="archive__item-body">
          {% if post.title %}

          <p class="archive__item-title">
	        <i style="margin-right: 0.5em;" class="fas fa-check"></i> <a href="{{ post.url }}"> {{ post.title }}</a></p>
          {% endif %}

          <div class="archive__item-excerpt">
		<i style="margin-right: 1em;" class="far fa-calendar-alt"></i> <span>{{ post.date | date:"%B %d, %Y"  }} </span>
          </div>

          {% if post.excerpt %}
            <div class="archive__item-excerpt">
              <p><i style="margin-right: 0.8em;" class="fas fa-comment"></i> {{ post.excerpt }} </p>
            </div>
          {% endif %}

        </div>
      </div>
    </div>
